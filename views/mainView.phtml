<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Main</title>
</head>
<body>
    
<a href="index.php?verPedido=true">Ver Pedido en Proceso</a>

    <h1>Lista de Productos</h1>
    <?php if (!empty($products)): ?>
        <table border="1">
            <tr>
                <th>Nombre</th>
                <th>Precio</th>
                <th>Descripción</th>
                <th>Stock</th>
                <th>Acciones</th>
            </tr>
            <?php foreach ($products as $product): ?>
                <tr>
                    <td><?php echo htmlspecialchars($product->getName()); ?></td>
                    <td><?php echo htmlspecialchars($product->getprice()); ?></td>
                    <td><?php echo htmlspecialchars($product->getDescription()); ?></td>
                    <td><?php echo htmlspecialchars($product->getStock()); ?></td>
                    <td>
                        <a href="index.php?addLP=<?php echo $product->getId(); ?>">Añadir al pedido</a>
                    </td>
                </tr>
            <?php endforeach; ?>
        </table>
    <?php else: ?>
        <p>No hay productos disponibles.</p>
    <?php endif; ?>
</body>
</html>
